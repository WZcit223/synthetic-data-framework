# Core shell: NO dependencies (Python 3.9+ standard library only).
#
# Optional extras, by phase (see docs/ROADMAP.md). Uncomment as adopted.

# --- Optional: HTTP API + dashboard (Phase 1) ---
# fastapi
# uvicorn

# --- Optional: real synthesis + validation (Phase 2.1) ---
# Enables `sdf.cli sdv`: Gaussian-copula synthesis + real SDMetrics scoring.
# copulas
# sdmetrics
# pandas
# numpy
# sdv        # adds CTGAN/TVAE (pulls torch) — heavier, for complex joints
# faker

# --- Optional: application algorithms (Phase 3) ---
# scikit-learn
# lightgbm
