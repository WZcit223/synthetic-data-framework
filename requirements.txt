# Core runtime dependencies (see pyproject.toml [project].dependencies).
# Install the package instead of this file: pip install -e .
numpy>=1.26
scipy>=1.11
scikit-learn>=1.4

# Optional extras, by concern (pip install ".[<extra>]"):
#   api             fastapi uvicorn
#   synthesis       copulas sdmetrics pandas
#   synthesis-deep  sdv faker            (CTGAN/TVAE; pulls torch)
#   app             lightgbm
#   causal          dowhy econml         (pywhy causal stack)
#   dev             pytest ruff
